<Window x:Class="EditorWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EditorWpf"
        mc:Ignorable="d"
        Title="Maxwin Editor" Height="720" Width="1200"
        MinHeight="480" MinWidth="900"
        Background="#141416" Foreground="#E7E7E7">
    <Grid>
        <Grid.Resources>
            <SolidColorBrush x:Key="PanelBg" Color="#1D1E22"/>
            <SolidColorBrush x:Key="PanelBorder" Color="#2A2B31"/>
            <SolidColorBrush x:Key="Accent" Color="#3B82F6"/>
            <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <local:InverseBooleanConverter x:Key="InverseBoolToVisibilityConverter"/>
            <Style TargetType="Button">
                <Setter Property="Background" Value="#262830"/>
                <Setter Property="Foreground" Value="#E7E7E7"/>
                <Setter Property="BorderBrush" Value="#3A3C44"/>
                <Setter Property="Padding" Value="10,6"/>
                <Setter Property="Margin" Value="0,0,8,0"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Background" Value="#1B1C20"/>
                <Setter Property="Foreground" Value="#E7E7E7"/>
                <Setter Property="BorderBrush" Value="#343640"/>
                <Setter Property="Padding" Value="6,4"/>
            </Style>
            <Style TargetType="ListBox">
                <Setter Property="Background" Value="#17181C"/>
                <Setter Property="Foreground" Value="#E7E7E7"/>
                <Setter Property="BorderThickness" Value="0"/>
            </Style>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="#E7E7E7"/>
            </Style>
            <Style x:Key="DetailsTextBoxStyle" TargetType="TextBox">
                <Setter Property="Height" Value="22"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Background" Value="#15161A"/>
                <Setter Property="BorderBrush" Value="#2C2E36"/>
                <Setter Property="Foreground" Value="#E7E7E7"/>
            </Style>
            <Style x:Key="DetailsCheckBoxStyle" TargetType="CheckBox">
                <Setter Property="Height" Value="22"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Foreground" Value="#E7E7E7"/>
                <Setter Property="Background" Value="#1F2127"/>
                <Setter Property="BorderBrush" Value="#4A4D59"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="4,0,0,0"/>
                <Setter Property="OverridesDefaultStyle" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="CheckBox">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="18"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border x:Name="Box" Width="14" Height="14" Margin="2"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="2"/>
                                <TextBlock Grid.Column="1" Text="{TemplateBinding Content}"
                                           VerticalAlignment="Center"
                                           Foreground="{TemplateBinding Foreground}"/>
                                <Path x:Name="Check" Grid.Column="0" Margin="4,4,0,0"
                                      Stroke="#E7E7E7" StrokeThickness="2"
                                      Data="M 0 4 L 4 8 L 10 0" Visibility="Collapsed"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Check" Property="Visibility" Value="Visible"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Box" Property="BorderBrush" Value="#6A6D78"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Box" Property="Opacity" Value="0.5"/>
                                    <Setter Property="Foreground" Value="#8A8D96"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="ResetIconButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="22"/>
                <Setter Property="Height" Value="22"/>
                <Setter Property="FontFamily" Value="Segoe UI Symbol"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Foreground" Value="#F2F2F4"/>
                <Setter Property="Background" Value="#262830"/>
                <Setter Property="BorderBrush" Value="#3A3C44"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="OverridesDefaultStyle" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="2">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#2A2C34"/>
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="#4A4D59"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#22242B"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.6"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style TargetType="DataGrid">
                <Setter Property="Background" Value="#17181C"/>
                <Setter Property="Foreground" Value="#E7E7E7"/>
                <Setter Property="RowBackground" Value="#17181C"/>
                <Setter Property="AlternatingRowBackground" Value="#1B1C21"/>
                <Setter Property="BorderBrush" Value="#2A2B31"/>
            </Style>
            <Style TargetType="DataGridRow">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsOverridden}" Value="True">
                        <Setter Property="Background" Value="#2A2330"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="8" LastChildFill="True">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button x:Name="OpenButton" Width="110" Click="OpenButton_OnClick">Open Scene</Button>
                <Button x:Name="ReloadButton" Width="80" Click="ReloadButton_OnClick">Reload</Button>
                <Button x:Name="SaveButton" Width="80" Click="SaveButton_OnClick">Save</Button>
            </StackPanel>
            <TextBox x:Name="ScenePathBox" IsReadOnly="True" VerticalContentAlignment="Center"/>
        </DockPanel>

        <Grid Grid.Row="1" Margin="8,0,8,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="320"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource PanelBg}" BorderBrush="{StaticResource PanelBorder}" BorderThickness="1" Padding="8">
                <DockPanel LastChildFill="True">
                    <TextBlock DockPanel.Dock="Top" FontWeight="Bold" Margin="0,0,0,6">Inspector</TextBlock>
                    <ListBox x:Name="InspectorList" FontFamily="Consolas"/>
                </DockPanel>
            </Border>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                          Background="#2A2B31" ResizeBehavior="PreviousAndNext" ResizeDirection="Columns"/>

            <Border Grid.Column="2" Background="#0F1013" BorderBrush="{StaticResource PanelBorder}" BorderThickness="1" Padding="8">
                <DockPanel LastChildFill="True">
                    <TextBlock DockPanel.Dock="Top" FontWeight="Bold" Margin="0,0,0,6">Game View</TextBlock>
                    <Border Background="#0B0C0F" BorderBrush="#1E2027" BorderThickness="1" CornerRadius="6">
                        <Grid>
                            <TextBlock Text="Game Window (placeholder)" HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Foreground="#8B8E98"/>
                        </Grid>
                    </Border>
                </DockPanel>
            </Border>

            <GridSplitter Grid.Column="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                          Background="#2A2B31" ResizeBehavior="PreviousAndNext" ResizeDirection="Columns"/>

            <Border Grid.Column="4" Background="{StaticResource PanelBg}" BorderBrush="{StaticResource PanelBorder}" BorderThickness="1" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="6"/>
                        <RowDefinition Height="240"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" FontWeight="Bold" Margin="0,0,0,6">Entities</TextBlock>
                    <ListBox Grid.Row="1" x:Name="EntitiesList" SelectionChanged="EntitiesList_OnSelectionChanged"
                             DisplayMemberPath="DisplayName"/>
                    <GridSplitter Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#2A2B31"/>

                    <Border Grid.Row="3" Background="#17181C" BorderBrush="#2A2B31" BorderThickness="1" Padding="6">
                        <DockPanel LastChildFill="True">
                            <TextBlock DockPanel.Dock="Top" FontWeight="Bold" Margin="0,0,0,6">Details</TextBlock>
                            <TreeView x:Name="DetailsTree" BorderThickness="0" FontSize="11" Background="#141519"
                                      Grid.IsSharedSizeScope="True"
                                      ScrollViewer.CanContentScroll="False"
                                      ScrollViewer.PanningMode="VerticalOnly"
                                      ScrollViewer.PanningRatio="0.2"
                                      ScrollViewer.PanningDeceleration="0.001"
                                      ScrollViewer.IsDeferredScrollingEnabled="True"
                                      PreviewMouseWheel="DetailsTree_OnPreviewMouseWheel">
                                <TreeView.Resources>
                                    <HierarchicalDataTemplate DataType="{x:Type local:ComponentNode}" ItemsSource="{Binding Fields}">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="#F0F0F2" Margin="0,6,0,4"/>
                                    </HierarchicalDataTemplate>
                                    <DataTemplate DataType="{x:Type local:FieldNode}">
                                        <Grid Margin="2,2" MinHeight="24">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" SharedSizeGroup="DetailName"/>
                                                <ColumnDefinition Width="*" SharedSizeGroup="DetailValue"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" Text="{Binding Name}" Foreground="#D0D2D8"/>

                                            <Grid Grid.Column="1" Margin="8,0,0,0" HorizontalAlignment="Stretch">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBox Grid.Column="0" FontSize="11" HorizontalAlignment="Stretch"
                                                         Style="{StaticResource DetailsTextBoxStyle}"
                                                         Visibility="{Binding IsBool, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                                                         Text="{Binding ValueText, UpdateSourceTrigger=LostFocus}"
                                                         LostFocus="DetailsValue_OnLostFocus"
                                                         KeyDown="DetailsValue_OnKeyDown"/>

                                                <CheckBox Grid.Column="0" VerticalAlignment="Center"
                                                          Style="{StaticResource DetailsCheckBoxStyle}"
                                                          Visibility="{Binding IsBool, Converter={StaticResource BoolToVisibilityConverter}}"
                                                          IsChecked="{Binding BoolValue}"
                                                          Click="DetailsBool_OnClick"/>

                                                <Button Grid.Column="1" Content="↺" Margin="6,0,0,0"
                                                        Style="{StaticResource ResetIconButtonStyle}"
                                                        ToolTip="Reset to prefab value"
                                                        Visibility="{Binding CanReset, Converter={StaticResource BoolToVisibilityConverter}}"
                                                        Click="DetailsReset_OnClick"/>
                                            </Grid>
                                        </Grid>
                                    </DataTemplate>
                                </TreeView.Resources>
                                <TreeView.ItemContainerStyle>
                                    <Style TargetType="TreeViewItem">
                                        <Style.Setters>
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Setter Property="Focusable" Value="False"/>
                                            <Setter Property="IsTabStop" Value="False"/>
                                            <Setter Property="Padding" Value="0"/>
                                            <EventSetter Event="Expanded" Handler="DetailsTree_OnExpanded"/>
                                            <EventSetter Event="Collapsed" Handler="DetailsTree_OnCollapsed"/>
                                        </Style.Setters>
                                        <Style.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="Foreground" Value="#E7E7E7"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </TreeView.ItemContainerStyle>
                            </TreeView>
                        </DockPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#1A1B1F" Padding="8,6">
            <TextBlock x:Name="StatusText" Foreground="#C9CBD2">Ready.</TextBlock>
        </Border>
    </Grid>
</Window>
