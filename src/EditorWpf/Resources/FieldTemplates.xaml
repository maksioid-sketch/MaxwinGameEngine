<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:EditorWpf"
                    xmlns:controls="clr-namespace:EditorWpf.Controls">
    <HierarchicalDataTemplate DataType="{x:Type local:ComponentNode}" ItemsSource="{Binding Fields}">
        <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="#F0F0F2" Margin="0,6,0,4"/>
    </HierarchicalDataTemplate>

    <DataTemplate DataType="{x:Type local:FieldNode}">
        <controls:FieldEditorControl/>
    </DataTemplate>

    <Style x:Key="DetailsTreeItemStyle" TargetType="TreeViewItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Grid.IsSharedSizeScope" Value="True"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="#E7E7E7"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
